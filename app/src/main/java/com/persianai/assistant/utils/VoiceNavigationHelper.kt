package com.persianai.assistant.utils

import android.content.Context

/**
 * DISABLED: Navigation feature removed
 * This helper has been stubbed to remove navigation dependencies
 */
class VoiceNavigationHelper(private val context: Context) {
    
    fun startNavigation(route: Any?) {
        // Navigation disabled
    }
    
    fun speakInstruction(step: Any?) {
        // Navigation disabled
    }
    
    fun speakTrafficInfo(trafficLevel: String, delayMinutes: Int) {
        // Navigation disabled
    }
    
    fun speakSpeedLimit(speedLimit: Int, currentSpeed: Int) {
        // Navigation disabled
    }
    
    fun speakSpeedCameraAlert(distance: Int) {
        // Navigation disabled
    }
    
    fun speakParkingInfo(parkingName: String, availableSpots: Int, distance: Int) {
        // Navigation disabled
    }
}
